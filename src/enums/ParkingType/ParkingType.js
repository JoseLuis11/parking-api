//using simulated enum as object in order to use js file in migrations
const ParkingTypeEnum = {
  PUBLIC: 'public',
  PRIVATE: 'private',
  COURTESY: 'courtesy'
}

export default Object.freeze(ParkingTypeEnum);
